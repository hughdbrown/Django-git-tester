This Django application is meant just for testing django-git.

It does a minimal amount of stuff to run a web interface around django-git. If you wanted to do a full-fledged application, this wwould be a decent starting point.
